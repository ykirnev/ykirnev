#include <stdio.h>
#include <string.h>
#include <stlib.h>

int main(void)
{
    int mas[1];
    int x;
    scanf("%d", &x);
    mas[0] = x;
    int len = 1;
    while (x != 0)
    {
        int mas_copy = (int) malloc(len + 1);
        if 
    }
}