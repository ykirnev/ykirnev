#include <stdio.h>

int main (void)
{
    int mas[14];
    int n, max = 0, sm = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i ++)
    {
        scanf("%d", &mas[i]);
        sm = sm + mas[i];
    }
    for (int i = 0; i < 4782968; i ++)
    {
        i_copy = i;
        for (int j = 0; j < 14; j ++)
        {
            
        }
    }
}